import React, { Component } from 'react'

import PropTypes from 'prop-types'

// import Companypage from 'modules/Site/components/Pages/Companies/Company';
// import Topic from 'modules/Site/components/Pages/Companies/Company/Topics/Topic';
import Topic from 'modules/Site/components/Pages/Topics/Topic'

export default class ObzorPage extends Topic {
  // static propTypes = {
  // 	item: PropTypes.object.isRequired,
  // };
  // // static contextTypes = {
  // // };
  // // constructor(props){
  // // 	super(props);
  // // 	this.state = {
  // // 	};
  // // }
  // render(){
  // 	const {
  // 		item,
  // 		open,
  // 	} = this.props;
  // 	const {
  // 		id,
  // 		name,
  // 		short_text,
  // 		content: topicContent,
  // 	} = item;
  // 	let content;
  // 	if(!open){
  // 		content = <div dangerouslySetInnerHTML={{__html: short_text}}></div>
  // 	}
  // 	else{
  // 		content = <div dangerouslySetInnerHTML={{__html: topicContent}}></div>
  // 	}
  // 	return <div>
  // 		<h2>{name}</h2>
  // 		{content}
  // 	</div>
  // }
}
