import React from 'react'

// import { useCitiesQuery } from 'src/modules/gql/generated'

// export const CitiesContext = React.createContext([]);

/**
 * Подгружаем список городов для глобального использования
 */
const Cities: React.FC = () => {
  // const { error, data } = useCitiesQuery();

  return null
}

export default Cities
